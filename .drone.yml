pipeline:
  build:
    image: node:8.9
    commands:
      - npm install
      - npm test
  docker:
    image: plugins/docker
    repo: ${REGISTRY_HOST}/auto-build-node
    username: $REGISTRY_USER
    password: $REGISTRY_PASS
    tags: latest
    dockerfile: Dockerfile
    registry: $REGISTRY_HOST
